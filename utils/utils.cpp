#ifndef UTILS_HPP
#define UTILS_HPP

#include <string>

namespace Utils {
    // Function to check if a number is prime
    bool isPrime(int num);

    // Function to validate binary strings
    bool validateBinaryString(const std::string& binary_str);
}

#endif // UTILS_HPP
